import { EmptyState } from "@/components/ui/empty-state";

export default function AgentPage() {
  return (
    <EmptyState
      title="Agent Playground chega no R07"
      description="Conversa, execução de ferramentas e trace em tempo real serão implementados após o núcleo do agente (R02)."
    />
  );
}
