import { EmptyState } from "@/components/ui/empty-state";

export default function ReservationsPage() {
  return (
    <EmptyState
      title="Reservas chega no R05"
      description="Disponibilidade, criação, cancelamento e confirmação de reservas de áreas comuns."
    />
  );
}
