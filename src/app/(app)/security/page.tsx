import { EmptyState } from "@/components/ui/empty-state";

export default function SecurityPage() {
  return (
    <EmptyState
      title="Central de Segurança chega no R08"
      description="Eventos de segurança, ações negadas e visibilidade de rate limiting."
    />
  );
}
